services:
  dev:
    build:
      context: ../..
      dockerfile: examples/dev-workspace/Dockerfile
    volumes:
      - ../..:/app
    working_dir: /app
    stdin_open: true
    tty: true
    entrypoint: ["bash", "-lc", "pip install -q -e '.[dev]' && exec bash"]
